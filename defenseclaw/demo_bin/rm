#!/usr/bin/env python3
"""rm simulation: never calls a filesystem mutation API."""

from __future__ import annotations

import sys


def main() -> int:
    print(
        "FAKE RM SAFETY NET: deletion was not performed; the demo fixture remains intact.",
        file=sys.stderr,
    )
    return 3


if __name__ == "__main__":
    raise SystemExit(main())
